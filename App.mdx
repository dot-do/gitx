---
title: gitx.do
description: Git repository browser for gitx.do
---

import { DeveloperDashboard } from '@mdxui/cockpit'

export default function App() {
  return (
    <DeveloperDashboard
      config={{
        branding: {
          name: 'gitx.do',
          logo: <span className="font-bold">gitx.do</span>,
        },
        identity: {
          clientId: 'gitx',
          devMode: process.env.NODE_ENV === 'development',
        },
        routes: {
          overview: true,
          requests: false,
          keys: true,
          team: false,
          billing: false,
          settings: true,
          webhooks: true,
          database: false,
          vault: false,
        },
      }}
    />
  )
}
